# Example: https://github.com/koekeishiya/skhd/blob/master/examples/skhdrc
# Keywords: https://github.com/koekeishiya/skhd/issues/1

:: default : yabai -m config window_border off

# Important: `@` will capture _everything_ so make sure there is a way back to default mode defined
:: resize @ : yabai -m config window_border on

# Enter resize mode
alt - r ; resize

# Open rx
alt - p : rx

resize < escape ; default

resize < h :          yabai -m window --resize left:-5:0.0
resize < shift - l :  yabai -m window --resize left:5:0.0

resize < l :          yabai -m window --resize right:5:0.0
resize < shift - h :  yabai -m window --resize right:-5:0.0

# Open Terminal
alt - return : /Applications/Alacritty.app/Contents/MacOS/alacritty
cmd + alt - return : emacs --chdir $HOME

# Open Browser
shift + alt - return : /Applications/Firefox.app/Contents/MacOS/firefox

# Reload yabai
shift + alt - c : launchctl kickstart -k "gui/${UID}/homebrew.mxcl.yabai"

# Stop yabai + spacebar
shift + cmd + alt - c : brew services stop yabai; brew services stop spacebar

# Start yabai + spacebar
shift + cmd + alt - s : brew services start yabai; brew services start spacebar

# Flip the lightswitch
cmd + alt - l : $HOME/.local/bin/lightswitch

# Window focus navigation
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus stack.next || yabai -m window --focus south
alt - k : yabai -m window --focus stack.prev || yabai -m window --focus north
alt - l : yabai -m window --focus east

# Window warping
shift + alt - h : yabai -m window --warp west
shift + alt - j : yabai -m window --warp south
shift + alt - k : yabai -m window --warp north
shift + alt - l : yabai -m window --warp east

# Create a space and move the current window to it
cmd + alt - n : yabai -m space --create && \
		index=$(yabai -m query --spaces --display | jq 'map(select(."native-fullscreen" == 0))[-1].index') && \
		yabai -m window --space "${index}" && \
		yabai -m space --focus "${index}"

# Space focus navigation
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 6 : yabai -m space --focus 6
alt - 7 : yabai -m space --focus 7
alt - 8 : yabai -m space --focus 8
alt - 9 : yabai -m space --focus 9

alt - 0x1B : frame=$(yabai -m query --windows --window | jq '.frame') && w=$(echo $frame | jq '.w') && h=$(echo $frame | jq '.h') && \
             yabai -m window --resize abs:$(expr $w - 40):$(expr $h - 40) && \
             yabai -m window --move rel:20:20

shift + alt - 0x18 : frame=$(yabai -m query --windows --window | jq '.frame') && w=$(echo $frame | jq '.w') && h=$(echo $frame | jq '.h') && \
                     yabai -m window --resize abs:$(expr $w + 40):$(expr $h + 40) && \
                     yabai -m window --move rel:-20:-20

# send window to desktop and follow focus
shift + alt - 1 : yabai -m window --space  1; yabai -m space --focus 1
shift + alt - 2 : yabai -m window --space  2; yabai -m space --focus 2
shift + alt - 3 : yabai -m window --space  3; yabai -m space --focus 3
shift + alt - 4 : yabai -m window --space  4; yabai -m space --focus 4
shift + alt - 5 : yabai -m window --space  5; yabai -m space --focus 5
shift + alt - 6 : yabai -m window --space  6; yabai -m space --focus 6
shift + alt - 7 : yabai -m window --space  7; yabai -m space --focus 7
shift + alt - 8 : yabai -m window --space  8; yabai -m space --focus 8
shift + alt - 9 : yabai -m window --space  9; yabai -m space --focus 9

# toggle window split type
alt - e : yabai -m window --toggle split

# zoom fullscreen
alt - f : yabai -m window --toggle zoom-fullscreen

# stack layout
alt - s : yabai -m window --stack next
alt - b : yabai -m space --layout bsp

# hide borders
shift + alt - b : yabai -m window --toggle border

# mirror tree y-axis
alt - y : yabai -m space --mirror y-axis

# mirror tree x-axis
alt - x : yabai -m space --mirror x-axis

# float / unfloat window and center on screen
alt - t : yabai -m window --toggle float;\
          yabai -m window --grid 7:7:1:1:5:5

# Close the window
alt - q [
  "discord" : yabai -m window --minimize; yabai -m wndow --focus recent
  * : yabai -m window --close; yabai -m window --focus recent
]

# Toggle music app focus
alt - m : yabai -m window --focus $(yabai -m query --windows --space mouse | jq -e '.[] | select(.app=="IINA" and .focused!=1) | .id' || echo 'recent')

.blacklist [
]
